# Contributing

1. Fork the repository and create a topic branch.
2. Run `npm install` followed by `npm run lint`, `npm run typecheck`, and `npm test`.
3. Include unit tests for new functionality and update documentation.
4. Open a pull request targeting `main` with a clear summary of changes.
