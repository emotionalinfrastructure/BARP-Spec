# Cultural Guidelines

- Default to explicit consent transitions and avoid implicit state changes.
- Preserve auditability by streaming JSONL logs to immutable storage.
- Use tolerance windows to respect timing nuance in emotional signals rather than hard cutoffs.
